{
  "name": "web-client",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "start": "nuxt dev --w",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "ts": "vue-tsc --noEmit",
    "lint:style": "stylelint 'src/**/*.{css,scss,vue}' --formatter verbose --fix --cache"
  },
  "dependencies": {
    "nuxt": "^3.13.2",
    "vue": "latest",
    "vue-router": "latest"
  },
  "devDependencies": {
    "@stylistic/stylelint-config": "^2.0.0",
    "api-contracts": "workspace:^",
    "postcss-html": "^1.7.0",
    "sass": "^1.80.6",
    "stylelint": "^16.10.0",
    "stylelint-config-clean-order": "^6.1.0",
    "stylelint-config-recommended-vue": "^1.5.0",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-config-standard-scss": "^13.1.0",
    "vue-tsc": "^2.1.10"
  }
}
